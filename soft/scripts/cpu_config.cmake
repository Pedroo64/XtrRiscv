if(NOT DEFINED EXECUTE_BYPASS)
    set(EXECUTE_BYPASS TRUE)
endif()
if(NOT DEFINED MEMORY_BYPASS)
    set(MEMORY_BYPASS TRUE)
endif()
if(NOT DEFINED WRITEBACK_BYPASS)
    set(WRITEBACK_BYPASS TRUE)
endif()
if(NOT DEFINED REGFILE_BYPASS)
    set(REGFILE_BYPASS TRUE)
endif()
if(NOT DEFINED FULL_BARREL_SHIFTER)
    set(FULL_BARREL_SHIFTER TRUE)
endif()
if(NOT DEFINED SHIFTER_EARLY_INJECTION)
    set(SHIFTER_EARLY_INJECTION TRUE)
endif()
if(NOT DEFINED STOP_TIME)
    set(STOP_TIME 1ms)
endif()
